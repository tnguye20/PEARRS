package tnguye20.pearrs;

import com.android.volley.RequestQueue;
import com.android.volley.toolbox.StringRequest;
import com.android.volley.toolbox.Volley;

/**
 * Created by Thang Nguyen on 11/5/2016.
 * This class would be used to get all the question at once using SQL
 */

public class Survey {
    public Survey(String nextSurvey){

        /*
        SurveyRequest surveyRequest = new SurveyRequest(nextSurvey, responseListener);
        RequestQueue queue = Volley.newRequestQueue(Survey.this);
        queue.add(surveyRequest);
        */
    }
}
